#include <stdio.h>

/**
 * main - causes a infinite loop
 * Return: 0 (success)
 */

int main(void)
{
	int i;

	printf("Infinite loop incoming :(\n");

	i = 0;
	/*
	 * while (i < 10)
	 * {
	 * _putchar(i)
	 * }
	 */
	printf("Infinite loop avoided! \\0/\n");

	return (0);
}
