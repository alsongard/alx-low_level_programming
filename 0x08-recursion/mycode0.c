#include "main.h"
/**
 * _puts_recursion - print string and new line
 * @s: pointer to the string
 * Return: (0);
 */
void _puts_recursion(char *s)
{

	if (a !='\0')
	{
		_putchar(s[a]);
		a++;
	}
	_putchar('\n');
}
