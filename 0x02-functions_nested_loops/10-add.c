#include "main.h"
/**
 * add - add two numbers
 *
 * @n: the two numbers to add
 * Description -  add two numbers
 * Return: (Sum)
 */

int add(int num1, int num2)
{
	int sum;

	sum =  num1 + num2;
	_putchar(sum + '0');
	return (sum);
}
